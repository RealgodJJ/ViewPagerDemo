<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_tab_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/lv_main_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        tools:ignore="UseCompoundDrawables">

        <ImageView
            android:id="@+id/iv_tab_icon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginTop="5dp"
            tools:ignore="ContentDescription"
            tools:src="@drawable/tabbar_home_style" />

        <TextView
            android:id="@+id/tv_tab_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:textColor="@color/text_color"
            tools:text="@string/main_page" />

    </LinearLayout>

    <ImageView
        android:id="@+id/iv_prompt"
        android:layout_width="2dp"
        android:layout_height="2dp"
        android:layout_above="@+id/lv_main_content"
        android:layout_toEndOf="@id/lv_main_content"
        android:visibility="gone"
        tools:ignore="ContentDescription" />

</RelativeLayout>